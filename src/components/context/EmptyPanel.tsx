import { EmptyState } from "@/components/ui/EmptyState";

export function EmptyPanel() {
  return (
    <EmptyState
      title="Portfolio context"
      description="Ask about Ayush’s background, projects, skills, or experience and the matching panel will appear here."
    />
  );
}
